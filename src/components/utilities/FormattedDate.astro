---
interface Props {
	date: Date
}

const { date } = Astro.props as Props
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('sk-SK', {
			year: 'numeric',
			month: 'long',
			day: 'numeric',
		})
	}
</time>
